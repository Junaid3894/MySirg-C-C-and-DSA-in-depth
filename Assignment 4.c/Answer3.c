#include<stdio.h>
int main()
{
    int a;
    float b;
    char c;
    double d;
    printf("Size of int is %d\nSize of float is \nsize of char is %d\nsize of double is %d",sizeof(a),sizeof(b),sizeof(c),sizeof(d));
    return 0;
}
